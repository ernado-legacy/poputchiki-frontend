.chatBlock(data-user="#{dialog.get('id')}")
  img(src='#{dialog.get("avatar_url")}')
  .topPart
    .chatName=dialog.get('name')
    .invite пригласить в путешествие
    .closeChat закрыть
  .centerPart
  .bottomPart.clearfix
    input(type='text')
    .pstMsg