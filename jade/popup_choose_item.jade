each array in arrays
  .imgRow.clearfix
    each item in array
      .imgBox.clearfix(data-id="#{item.get('id')}", data-video="#{video}")
        img(src='#{item.get('thumbnail_url')}')
        .cover
        span выбрать