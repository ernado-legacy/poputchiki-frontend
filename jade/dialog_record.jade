div(class = invite===true ? 'record invite':'record')
  .recordTime=time
  if invite
    .fui-radio-checked
  .recordAuthor=author
  if photo_url
      .messageimg
        img(src=photo_url)
  
  .recordText=text